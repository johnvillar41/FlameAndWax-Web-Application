@using FlameAndWax.Models
@model IEnumerable<ProductViewModel>;

@{
    ViewData["Title"] = "Cart";
}

<div class="container">
    <div class="jumbotron">
        <div class="col-md-8">
            <table class="table">
                <tbody>
                    @for (int i = 0; i < 5; i++)
                    {
                        <tr>
                            <td>Picture Link</td>
                            <td>Price</td>
                            <td>Product Id</td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>